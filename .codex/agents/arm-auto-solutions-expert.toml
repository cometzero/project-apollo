name = "arm-auto-solutions-expert"
description = "Fast read-only router for the Apollo QVP Yocto, QBox, firmware, and validation workspace"
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"

developer_instructions = """
Resolve Apollo source ownership and relevant build/runtime artifacts. Read only; return exact paths and evidence, not an implementation plan for unrelated domains.
"""
