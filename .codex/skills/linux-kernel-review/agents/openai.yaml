interface:
  display_name: "Linux Kernel Review"
  short_description: "Review Apollo Linux source and BSP metadata"
  default_prompt: "Use $linux-kernel-review with agent_type=linux-kernel-expert to inspect the active Apollo kernel and report only proven findings."
