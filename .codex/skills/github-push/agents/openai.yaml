interface:
  display_name: "GitHub Push"
  short_description: "Safely push the superproject and owned submodules"
  default_prompt: "Use $github-push to audit, dry-run, and publish eligible committed refs to my GitHub remotes."
