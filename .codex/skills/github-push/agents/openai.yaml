interface:
  display_name: "GitHub Push"
  short_description: "Safely push the superproject and owned submodules"
  default_prompt: "Use $github-push under the project gpt-5.6-sol default to audit, dry-run, and push eligible committed refs to my GitHub remotes."
