interface:
  display_name: "Yocto Dev"
  short_description: "Develop Apollo Yocto metadata on the 5.2.4 baseline"
  default_prompt: "Use $yocto-dev with agent_type=yocto_dev to identify the owning Apollo layer and validate the narrowest BitBake task first."
