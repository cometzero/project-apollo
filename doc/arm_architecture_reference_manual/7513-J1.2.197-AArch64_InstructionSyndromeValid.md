# ​J1.2.197 AArch64_InstructionSyndromeValid

Source: <https://developer.arm.com/documentation/ddi0487/mc/-Part-J-Architectural-Pseudocode/-Chapter-J1-A-profile-Architecture-Pseudocode/-J1-2-Pseudocode-for-AArch64-operation/-J1-2-197-AArch64-InstructionSyndromeValid>

##### J1.2.197 AArch64\_InstructionSyndromeValid

```
// AArch64_InstructionSyndromeValid()
// ==================================
// Returns TRUE if ESR_ELx.ISV is ‘1’ for the given Fault.
func AArch64_InstructionSyndromeValid(fault : FaultRecord) => boolean
begin
    if !fault.accessdesc.lowestaddress then
        return FALSE;
    end;
    if fault.s2fs1walk then
        return FALSE;
    end;
    if IsFeatureImplemented(FEAT_RAS) && IsExternalSyncAbort(fault) then
        return FALSE;
    end;
    if fault.accessdesc.ls64 then
        return fault.statuscode IN {Fault_AccessFlag, Fault_Translation, Fault_Permission};
    end;
    if fault.accessdesc.acctype == AccessType_GCS then
        return FALSE;
    end;
    if IsSecondStage(fault) then
        return (!IsExternalAbortOnWalk(fault) ||
                ImpDefBool(“ISV on second stage translation table walk”));
    end;
    return FALSE;
end;
```
