# ​C8.2.340 LD1SB (scalar plus vector)

Source: <https://developer.arm.com/documentation/ddi0487/mc/-Part-C-The-AArch64-Instruction-Set/-Chapter-C8-SVE-Instruction-Descriptions/-C8-2-Alphabetical-list-of-SVE-instructions/-C8-2-340-LD1SB--scalar-plus-vector->

##### C8.2.340 LD1SB (scalar plus vector)

Gather load signed bytes to vector (vector index)

This instruction performs a gather load of signed bytes to Active elements of a vector register from memory addresses generated by a 64-bit scalar base plus vector index. The index values are optionally sign-extended or zero-extended from 32 to 64 bits. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector.

This instruction is illegal when executed in Streaming SVE mode, unless FEAT\_SME\_FA64 is implemented and enabled.

It has encodings from 3 classes: [32-bit unpacked unscaled offset](/documentation/ddi0487/mc/-Part-C-The-AArch64-Instruction-Set/-Chapter-C8-SVE-Instruction-Descriptions/-C8-2-Alphabetical-list-of-SVE-instructions/-C8-2-340-LD1SB--scalar-plus-vector-?lang=en#isa_ld1sb_z_p_bz_iclass_32_bit_unpacked_unscaled_offset), [32-bit unscaled offset](/documentation/ddi0487/mc/-Part-C-The-AArch64-Instruction-Set/-Chapter-C8-SVE-Instruction-Descriptions/-C8-2-Alphabetical-list-of-SVE-instructions/-C8-2-340-LD1SB--scalar-plus-vector-?lang=en#isa_ld1sb_z_p_bz_iclass_32_bit_unscaled_offset), and [64-bit unscaled offset](/documentation/ddi0487/mc/-Part-C-The-AArch64-Instruction-Set/-Chapter-C8-SVE-Instruction-Descriptions/-C8-2-Alphabetical-list-of-SVE-instructions/-C8-2-340-LD1SB--scalar-plus-vector-?lang=en#isa_ld1sb_z_p_bz_iclass_64_bit_unscaled_offset)

###### 32-bit unpacked unscaled offset

###### (FEAT\_SVE)

![](images/2003-C8.2.340-LD1SB-scalar-plus-vector-img01.svg)

###### Encoding

```
LD1SB  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D, <mod>]
```

###### Decode for this encoding

```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 8;
let offs_size : integer{} = 32;
let unsigned : boolean = FALSE;
let offs_unsigned : boolean = xs == ‘0’;
let scale : integer = 0;
```

###### 32-bit unscaled offset

###### (FEAT\_SVE)

![](images/2003-C8.2.340-LD1SB-scalar-plus-vector-img02.svg)

###### Encoding

```
LD1SB  { <Zt>.S }, <Pg>/Z, [<Xn|SP>, <Zm>.S, <mod>]
```

###### Decode for this encoding

```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 32;
let msize : integer{} = 8;
let offs_size : integer{} = 32;
let unsigned : boolean = FALSE;
let offs_unsigned : boolean = xs == ‘0’;
let scale : integer = 0;
```

###### 64-bit unscaled offset

###### (FEAT\_SVE)

![](images/2003-C8.2.340-LD1SB-scalar-plus-vector-img03.svg)

###### Encoding

```
LD1SB  { <Zt>.D }, <Pg>/Z, [<Xn|SP>, <Zm>.D]
```

###### Decode for this encoding

```
if !IsFeatureImplemented(FEAT_SVE) then EndOfDecode(Decode_UNDEF); end;
let t : integer = UInt(Zt);
let n : integer = UInt(Rn);
let m : integer = UInt(Zm);
let g : integer = UInt(Pg);
let esize : integer{} = 64;
let msize : integer{} = 8;
let offs_size : integer{} = 64;
let unsigned : boolean = FALSE;
let offs_unsigned : boolean = TRUE;
let scale : integer = 0;
```

###### Assembler Symbols

<Zt>
:   ​

    Is the name of the scalable vector register to be transferred, encoded in the “Zt” field.

<Pg>
:   ​

    Is the name of the governing scalable predicate register P0-P7, encoded in the “Pg” field.

<Xn|SP>
:   ​

    Is the 64-bit name of the general-purpose base register or stack pointer, encoded in the “Rn” field.

<Zm>
:   ​

    Is the name of the offset scalable vector register, encoded in the “Zm” field.

<mod>
:   ​
    ​

    Is the index extend and shift specifier, encoded in “xs”:

<table>
 <colgroup>
  <col/>
  <col/>
 </colgroup>
 <thead>
  <tr>
   <th>
    <strong>
     xs
    </strong>
   </th>
   <th>
    <strong>
     &lt;mod&gt;
    </strong>
   </th>
  </tr>
 </thead>
 <tbody>
  <tr>
   <td>
    <p>
     <code>
      0
     </code>
    </p>
   </td>
   <td>
    <p>
     <code>
      UXTW
     </code>
    </p>
   </td>
  </tr>
  <tr>
   <td>
    <p>
     <code>
      1
     </code>
    </p>
   </td>
   <td>
    <p>
     <code>
      SXTW
     </code>
    </p>
   </td>
  </tr>
 </tbody>
</table>

###### Operation

```
CheckNonStreamingSVEEnabled();
let VL : integer{} = CurrentVL();
let PL : integer{} = VL DIV 8;
let elements : integer = VL DIV esize;
var base : bits(64);
let mask : bits(PL) = P{}(g);
var offset : bits(VL);
var result : bits(VL);
var data : bits(msize);
let mbytes : integer{} = msize DIV 8;
let contiguous : boolean = FALSE;
let nontemporal : boolean = FALSE;
let predicated : boolean = TRUE;
let tagchecked : boolean = TRUE;
let accdesc : AccessDescriptor = CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,
                                                     predicated, tagchecked);
if !AnyActiveElement{PL}(mask, esize) then
    if n == 31 && ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE) then
        CheckSPAlignment();
    end;
else
    if n == 31 then CheckSPAlignment(); end;
    base = if n == 31 then SP{64}() else X{64}(n);
    offset = Z{VL}(m);
end;
for e = 0 to elements-1 do
    if ActivePredicateElement{PL}(mask, e, esize) then
        let offselt : bits(offs_size) = offset[e*:esize][offs_size-1:0];
        let off : integer = if offs_unsigned then UInt(offselt) else SInt(offselt);
        let addr : bits(64) = AddressAdd(base, off << scale, accdesc);
        data = Mem{msize}(addr, accdesc);
        result[e*:esize] = Extend{esize}(data, unsigned);
    else
        result[e*:esize] = Zeros{esize};
    end;
end;
Z{VL}(t) = result;
```

###### Operational Information

This instruction is a data-independent-time instruction as described in [About PSTATE.DIT](/documentation/ddi0487/mc/-Part-B-The-AArch64-Application-Level-Architecture/-Chapter-B1-The-AArch64-Application-Level-Programmers--Model/-B1-5-Software-control-features-and-EL0/-B1-5-6-About-PSTATE-DIT?lang=en#beiccddab3).
