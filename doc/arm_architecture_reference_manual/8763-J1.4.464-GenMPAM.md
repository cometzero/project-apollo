# ​J1.4.464 GenMPAM

Source: <https://developer.arm.com/documentation/ddi0487/mc/-Part-J-Architectural-Pseudocode/-Chapter-J1-A-profile-Architecture-Pseudocode/-J1-4-Shared-pseudocode/-J1-4-464-GenMPAM>

##### J1.4.464 GenMPAM

```
// GenMPAM()
// =========
// Returns MPAMinfo for Exception level el.
// If mpamdata.sm is TRUE returns MPAM information using MPAMSM_EL1.
// Otherwise returns MPAM information using MPAMn_ELx.
func GenMPAM(el_in : bits(2), mpamdata : MPAMdata) => MPAMinfo
begin
    var returninfo : MPAMinfo;
    var partidel : PARTIDType;
    var perr : boolean;
    var el : bits(2) = el_in;
    // Check if the guest OS application is locked by the EL2 hypervisor to
    // only use the EL1 virtual machine’s PARTIDs.
    if (el == EL0 && EL2Enabled() && MPAMHCR_EL2().GSTAPP_PLK == ‘1’ &&
          HCR_EL2().TGE == ‘0’) then
        el = EL1;
    end;
    (partidel, perr)      = GenPARTID(el, mpamdata);
    let groupel : PMGType = GenPMG(el, mpamdata, perr);
    returninfo.partid  = partidel;
    returninfo.pmg     = groupel;
    return returninfo;
end;
```
