# ​J1.2.305 CheckSPAlignment

Source: <https://developer.arm.com/documentation/ddi0487/mc/-Part-J-Architectural-Pseudocode/-Chapter-J1-A-profile-Architecture-Pseudocode/-J1-2-Pseudocode-for-AArch64-operation/-J1-2-305-CheckSPAlignment>

##### J1.2.305 CheckSPAlignment

```
// CheckSPAlignment()
// ==================
// Check correct stack pointer alignment for AArch64 state.
func CheckSPAlignment()
begin
    let sp : bits(64) = SP{}();
    var stack_align_check : boolean;
    if PSTATE.EL == EL0 then
        stack_align_check = (SCTLR_ELx().SA0 != ‘0’);
    else
        stack_align_check = (SCTLR_ELx().SA != ‘0’);
    end;
    if stack_align_check && sp != AlignDownSize(sp, 16) then
        AArch64_SPAlignmentFault();
    end;
    return;
end;
```
