# ​C5.9.2 GCSPOPM, Guarded Control Stack Pop

Source: <https://developer.arm.com/documentation/ddi0487/mc/-Part-C-The-AArch64-Instruction-Set/-Chapter-C5-The-A64-System-Instruction-Class/-C5-9-A64-System-instructions-for-the-Guarded-Control-Stack/-C5-9-2-GCSPOPM--Guarded-Control-Stack-Pop>

##### C5.9.2 GCSPOPM, Guarded Control Stack Pop

The GCSPOPM characteristics are:

Purpose
:   Loads the 64-bit doubleword that is pointed to by the current GCS pointer register, writes it to the destination register, and increments the current GCS pointer register by the size of a GCS procedure return record.

Configuration
:   This system instruction is present only when FEAT\_GCS is implemented and FEAT\_AA64 is implemented. Otherwise, direct accesses to GCSPOPM are UNDEFINED.

Attributes
:   GCSPOPM is a 64-bit System instruction.

###### Field descriptions

![](images/0666-C5.9.2-GCSPOPM-Guarded-Control-Stack-Pop-img01.svg)

Output, bits [63:0]
:   Output value for GCS procedure return record.

###### Executing GCSPOPM

This system instruction is an alias of the SYSL instruction.

Accesses to this instruction use the following encodings in the System instruction encoding space:

###### `GCSPOPM {<Xt>}`

<table>
 <colgroup>
  <col/>
  <col/>
  <col/>
  <col/>
  <col/>
 </colgroup>
 <thead>
  <tr>
   <th>
    <strong>
     op0
    </strong>
   </th>
   <th>
    <strong>
     op1
    </strong>
   </th>
   <th>
    <strong>
     CRn
    </strong>
   </th>
   <th>
    <strong>
     CRm
    </strong>
   </th>
   <th>
    <strong>
     op2
    </strong>
   </th>
  </tr>
 </thead>
 <tbody>
  <tr>
   <td>
    <p>
     <code>
      0b01
     </code>
    </p>
   </td>
   <td>
    <p>
     <code>
      0b011
     </code>
    </p>
   </td>
   <td>
    <p>
     <code>
      0b0111
     </code>
    </p>
   </td>
   <td>
    <p>
     <code>
      0b0111
     </code>
    </p>
   </td>
   <td>
    <p>
     <code>
      0b001
     </code>
    </p>
   </td>
  </tr>
 </tbody>
</table>

```
if !(IsFeatureImplemented(FEAT_GCS) && IsFeatureImplemented(FEAT_AA64)) then
    Undefined();
elsif PSTATE.EL == EL0 then
    if GCSEnabled(EL0) then
        X{64}(t) = GCSPOPM();
    end;
elsif PSTATE.EL == EL1 then
    if GCSEnabled(EL1) then
        X{64}(t) = GCSPOPM();
    end;
elsif PSTATE.EL == EL2 then
    if GCSEnabled(EL2) then
        X{64}(t) = GCSPOPM();
    end;
elsif PSTATE.EL == EL3 then
    if GCSEnabled(EL3) then
        X{64}(t) = GCSPOPM();
    end;
end;
```
