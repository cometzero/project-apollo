# ​F6.1.134 VMOV (immediate)

Source: <https://developer.arm.com/documentation/ddi0487/mc/-Part-F-The-AArch32-Instruction-Sets/-Chapter-F6-T32-and-A32-Advanced-SIMD-and-Floating-point-Instruction-Descriptions/-F6-1-Alphabetical-list-of-Advanced-SIMD-and-floating-point-instructions/-F6-1-134-VMOV--immediate->

##### F6.1.134 VMOV (immediate)

Copy immediate value to a SIMD&FP register places an immediate constant into every element of the destination register.

Depending on settings in the [CPACR](/documentation/ddi0487/mc/-Part-G-The-AArch32-System-Level-Architecture/-Chapter-G8-AArch32-System-Register-Descriptions/-G8-2-General-system-control-registers/-G8-2-33-CPACR--Architectural-Feature-Access-Control-Register?lang=en#reg_aarch32_cpacr), [NSACR](/documentation/ddi0487/mc/-Part-G-The-AArch32-System-Level-Architecture/-Chapter-G8-AArch32-System-Register-Descriptions/-G8-2-General-system-control-registers/-G8-2-120-NSACR--Non-Secure-Access-Control-Register?lang=en#reg_aarch32_nsacr), [HCPTR](/documentation/ddi0487/mc/-Part-G-The-AArch32-System-Level-Architecture/-Chapter-G8-AArch32-System-Register-Descriptions/-G8-2-General-system-control-registers/-G8-2-64-HCPTR--Hyp-Architectural-Feature-Trap-Register?lang=en#reg_aarch32_hcptr), and [FPEXC](/documentation/ddi0487/mc/-Part-G-The-AArch32-System-Level-Architecture/-Chapter-G8-AArch32-System-Register-Descriptions/-G8-2-General-system-control-registers/-G8-2-54-FPEXC--Floating-Point-Exception-Control-register?lang=en#reg_aarch32_fpexc) registers, and the Security state and PE mode in which the instruction is executed, an attempt to execute the instruction might be UNDEFINED, or trapped to Hyp mode. For more information see [Enabling Advanced SIMD and floating-point support](/documentation/ddi0487/mc/-Part-G-The-AArch32-System-Level-Architecture/-Chapter-G1-The-AArch32-System-Level-Programmers--Model/-G1-21-Advanced-SIMD-and-floating-point-support/-G1-21-2-Enabling-Advanced-SIMD-and-floating-point-support?lang=en#cihiddff).

It has encodings from the following instruction sets: A32 ([A1](/documentation/ddi0487/mc/-Part-F-The-AArch32-Instruction-Sets/-Chapter-F6-T32-and-A32-Advanced-SIMD-and-Floating-point-Instruction-Descriptions/-F6-1-Alphabetical-list-of-Advanced-SIMD-and-floating-point-instructions/-F6-1-134-VMOV--immediate-?lang=en#isa_vmov_i_iclass_a1), [A2](/documentation/ddi0487/mc/-Part-F-The-AArch32-Instruction-Sets/-Chapter-F6-T32-and-A32-Advanced-SIMD-and-Floating-point-Instruction-Descriptions/-F6-1-Alphabetical-list-of-Advanced-SIMD-and-floating-point-instructions/-F6-1-134-VMOV--immediate-?lang=en#isa_vmov_i_iclass_a2), [A3](/documentation/ddi0487/mc/-Part-F-The-AArch32-Instruction-Sets/-Chapter-F6-T32-and-A32-Advanced-SIMD-and-Floating-point-Instruction-Descriptions/-F6-1-Alphabetical-list-of-Advanced-SIMD-and-floating-point-instructions/-F6-1-134-VMOV--immediate-?lang=en#isa_vmov_i_iclass_a3), [A4](/documentation/ddi0487/mc/-Part-F-The-AArch32-Instruction-Sets/-Chapter-F6-T32-and-A32-Advanced-SIMD-and-Floating-point-Instruction-Descriptions/-F6-1-Alphabetical-list-of-Advanced-SIMD-and-floating-point-instructions/-F6-1-134-VMOV--immediate-?lang=en#isa_vmov_i_iclass_a4), and [A5](/documentation/ddi0487/mc/-Part-F-The-AArch32-Instruction-Sets/-Chapter-F6-T32-and-A32-Advanced-SIMD-and-Floating-point-Instruction-Descriptions/-F6-1-Alphabetical-list-of-Advanced-SIMD-and-floating-point-instructions/-F6-1-134-VMOV--immediate-?lang=en#isa_vmov_i_iclass_a5)) and T32 ([T1](/documentation/ddi0487/mc/-Part-F-The-AArch32-Instruction-Sets/-Chapter-F6-T32-and-A32-Advanced-SIMD-and-Floating-point-Instruction-Descriptions/-F6-1-Alphabetical-list-of-Advanced-SIMD-and-floating-point-instructions/-F6-1-134-VMOV--immediate-?lang=en#isa_vmov_i_iclass_t1), [T2](/documentation/ddi0487/mc/-Part-F-The-AArch32-Instruction-Sets/-Chapter-F6-T32-and-A32-Advanced-SIMD-and-Floating-point-Instruction-Descriptions/-F6-1-Alphabetical-list-of-Advanced-SIMD-and-floating-point-instructions/-F6-1-134-VMOV--immediate-?lang=en#isa_vmov_i_iclass_t2), [T3](/documentation/ddi0487/mc/-Part-F-The-AArch32-Instruction-Sets/-Chapter-F6-T32-and-A32-Advanced-SIMD-and-Floating-point-Instruction-Descriptions/-F6-1-Alphabetical-list-of-Advanced-SIMD-and-floating-point-instructions/-F6-1-134-VMOV--immediate-?lang=en#isa_vmov_i_iclass_t3), [T4](/documentation/ddi0487/mc/-Part-F-The-AArch32-Instruction-Sets/-Chapter-F6-T32-and-A32-Advanced-SIMD-and-Floating-point-Instruction-Descriptions/-F6-1-Alphabetical-list-of-Advanced-SIMD-and-floating-point-instructions/-F6-1-134-VMOV--immediate-?lang=en#isa_vmov_i_iclass_t4), and [T5](/documentation/ddi0487/mc/-Part-F-The-AArch32-Instruction-Sets/-Chapter-F6-T32-and-A32-Advanced-SIMD-and-Floating-point-Instruction-Descriptions/-F6-1-Alphabetical-list-of-Advanced-SIMD-and-floating-point-instructions/-F6-1-134-VMOV--immediate-?lang=en#isa_vmov_i_iclass_t5)).

###### A1

![](images/6060-F6.1.134-VMOV-immediate-img01.svg)

###### Encoding for the 64-bit SIMD vector variant

Applies when `(Q == 0)`

```
VMOV{<c>}{<q>}.I32  <Dd>, #<imm>
```

###### Encoding for the 128-bit SIMD vector variant

Applies when `(Q == 1)`

```
VMOV{<c>}{<q>}.I32  <Qd>, #<imm>
```

###### Decode for all variants of this encoding

```
if op == ‘0’ && cmode[0] == ‘1’ && cmode[3:2] != ‘11’ then See(“VORR (immediate)”); end;
if op == ‘1’ && cmode != ‘1110’ then See(“Related encodings”); end;
if Q == ‘1’ && Vd[0] == ‘1’ then Undefined(); end;
let advsimd : boolean = TRUE;
let esize : integer{} = 64;
let imm : bits(esize) = AdvSIMDExpandImm(op, cmode, i::imm3::imm4);
let d : integer = UInt(D::Vd);
let regs : integer = if Q == ‘0’ then 1 else 2;
```

###### A2

![](images/6060-F6.1.134-VMOV-immediate-img02.svg)

###### Encoding for the Half-precision scalar variant

(FEAT\_FP16) Applies when `(size == 01)`

```
VMOV{<c>}{<q>}.F16  <Sd>, #<imm>
```

###### Encoding for the Single-precision scalar variant

Applies when `(size == 10)`

```
VMOV{<c>}{<q>}.F32  <Sd>, #<imm>
```

###### Encoding for the Double-precision scalar variant

Applies when `(size == 11)`

```
VMOV{<c>}{<q>}.F64  <Dd>, #<imm>
```

###### Decode for all variants of this encoding

```
if FPSCR().Len != ‘000’ || FPSCR().Stride != ‘00’ then Undefined(); end;
if size == ‘00’ || (size == ‘01’ && !IsFeatureImplemented(FEAT_FP16)) then Undefined(); end;
if size == ‘01’ && cond != ‘1110’ then UnpredictableProcedure(); end;
let esize : integer{} = 8 << UInt(size);
let imm : bits(esize) = VFPExpandImm{}(imm4H::imm4L);
let advsimd : boolean = FALSE;
let d : integer = if size == ‘11’ then UInt(D::Vd) else UInt(Vd::D);
let regs : integer = if size == ‘11’ then 1 else 0;
```

###### CONSTRAINED UNPREDICTABLE behavior

If `size == '01' && cond != '1110'`, then one of the following behaviors must occur:

- The instruction is UNDEFINED.
- The instruction executes as if it passes the Condition code check.
- The instruction executes as NOP. This means it behaves as if it fails the Condition code check.

###### A3

![](images/6060-F6.1.134-VMOV-immediate-img03.svg)

###### Encoding for the 64-bit SIMD vector variant

Applies when `(Q == 0)`

```
VMOV{<c>}{<q>}.I16  <Dd>, #<imm>
```

###### Encoding for the 128-bit SIMD vector variant

Applies when `(Q == 1)`

```
VMOV{<c>}{<q>}.I16  <Qd>, #<imm>
```

###### Decode for all variants of this encoding

```
if op == ‘0’ && cmode[0] == ‘1’ && cmode[3:2] != ‘11’ then See(“VORR (immediate)”); end;
if op == ‘1’ && cmode != ‘1110’ then See(“Related encodings”); end;
if Q == ‘1’ && Vd[0] == ‘1’ then Undefined(); end;
let advsimd : boolean = TRUE;
let esize : integer{} = 64;
let imm : bits(esize) = AdvSIMDExpandImm(op, cmode, i::imm3::imm4);
let d : integer = UInt(D::Vd);
let regs : integer = if Q == ‘0’ then 1 else 2;
```

###### A4

![](images/6060-F6.1.134-VMOV-immediate-img04.svg)

###### Encoding for the 64-bit SIMD vector variant

Applies when `(Q == 0)`

```
VMOV{<c>}{<q>}.<dt>  <Dd>, #<imm>
```

###### Encoding for the 128-bit SIMD vector variant

Applies when `(Q == 1)`

```
VMOV{<c>}{<q>}.<dt>  <Qd>, #<imm>
```

###### Decode for all variants of this encoding

```
if op == ‘0’ && cmode[0] == ‘1’ && cmode[3:2] != ‘11’ then See(“VORR (immediate)”); end;
if op == ‘1’ && cmode != ‘1110’ then See(“Related encodings”); end;
if Q == ‘1’ && Vd[0] == ‘1’ then Undefined(); end;
let advsimd : boolean = TRUE;
let esize : integer{} = 64;
let imm : bits(esize) = AdvSIMDExpandImm(op, cmode, i::imm3::imm4);
let d : integer = UInt(D::Vd);
let regs : integer = if Q == ‘0’ then 1 else 2;
```

###### A5

![](images/6060-F6.1.134-VMOV-immediate-img05.svg)

###### Encoding for the 64-bit SIMD vector variant

Applies when `(Q == 0)`

```
VMOV{<c>}{<q>}.I64  <Dd>, #<imm>
```

###### Encoding for the 128-bit SIMD vector variant

Applies when `(Q == 1)`

```
VMOV{<c>}{<q>}.I64  <Qd>, #<imm>
```

###### Decode for all variants of this encoding

```
if op == ‘0’ && cmode[0] == ‘1’ && cmode[3:2] != ‘11’ then See(“VORR (immediate)”); end;
if op == ‘1’ && cmode != ‘1110’ then See(“Related encodings”); end;
if Q == ‘1’ && Vd[0] == ‘1’ then Undefined(); end;
let advsimd : boolean = TRUE;
let esize : integer{} = 64;
let imm : bits(esize) = AdvSIMDExpandImm(op, cmode, i::imm3::imm4);
let d : integer = UInt(D::Vd);
let regs : integer = if Q == ‘0’ then 1 else 2;
```

###### T1

![](images/6060-F6.1.134-VMOV-immediate-img06.svg)

###### Encoding for the 64-bit SIMD vector variant

Applies when `(Q == 0)`

```
VMOV{<c>}{<q>}.I32  <Dd>, #<imm>
```

###### Encoding for the 128-bit SIMD vector variant

Applies when `(Q == 1)`

```
VMOV{<c>}{<q>}.I32  <Qd>, #<imm>
```

###### Decode for all variants of this encoding

```
if op == ‘0’ && cmode[0] == ‘1’ && cmode[3:2] != ‘11’ then See(“VORR (immediate)”); end;
if op == ‘1’ && cmode != ‘1110’ then See(“Related encodings”); end;
if Q == ‘1’ && Vd[0] == ‘1’ then Undefined(); end;
let advsimd : boolean = TRUE;
let esize : integer{} = 64;
let imm : bits(esize) = AdvSIMDExpandImm(op, cmode, i::imm3::imm4);
let d : integer = UInt(D::Vd);
let regs : integer = if Q == ‘0’ then 1 else 2;
```

###### T2

![](images/6060-F6.1.134-VMOV-immediate-img07.svg)

###### Encoding for the Half-precision scalar variant

(FEAT\_FP16) Applies when `(size == 01)`

```
VMOV{<c>}{<q>}.F16  <Sd>, #<imm>
```

###### Encoding for the Single-precision scalar variant

Applies when `(size == 10)`

```
VMOV{<c>}{<q>}.F32  <Sd>, #<imm>
```

###### Encoding for the Double-precision scalar variant

Applies when `(size == 11)`

```
VMOV{<c>}{<q>}.F64  <Dd>, #<imm>
```

###### Decode for all variants of this encoding

```
if FPSCR().Len != ‘000’ || FPSCR().Stride != ‘00’ then Undefined(); end;
if size == ‘00’ || (size == ‘01’ && !IsFeatureImplemented(FEAT_FP16)) then Undefined(); end;
if size == ‘01’ && InITBlock()  then UnpredictableProcedure(); end;
let esize : integer{} = 8 << UInt(size);
let imm : bits(esize) = VFPExpandImm{}(imm4H::imm4L);
let advsimd : boolean = FALSE;
let d : integer = if size == ‘11’ then UInt(D::Vd) else UInt(Vd::D);
let regs : integer = if size == ‘11’ then 1 else 0;
```

###### CONSTRAINED UNPREDICTABLE behavior

If `size == '01' && InITBlock()`, then one of the following behaviors must occur:

- The instruction is UNDEFINED.
- The instruction executes as if it passes the Condition code check.
- The instruction executes as NOP. This means it behaves as if it fails the Condition code check.

###### T3

![](images/6060-F6.1.134-VMOV-immediate-img08.svg)

###### Encoding for the 64-bit SIMD vector variant

Applies when `(Q == 0)`

```
VMOV{<c>}{<q>}.I16  <Dd>, #<imm>
```

###### Encoding for the 128-bit SIMD vector variant

Applies when `(Q == 1)`

```
VMOV{<c>}{<q>}.I16  <Qd>, #<imm>
```

###### Decode for all variants of this encoding

```
if op == ‘0’ && cmode[0] == ‘1’ && cmode[3:2] != ‘11’ then See(“VORR (immediate)”); end;
if op == ‘1’ && cmode != ‘1110’ then See(“Related encodings”); end;
if Q == ‘1’ && Vd[0] == ‘1’ then Undefined(); end;
let advsimd : boolean = TRUE;
let esize : integer{} = 64;
let imm : bits(esize) = AdvSIMDExpandImm(op, cmode, i::imm3::imm4);
let d : integer = UInt(D::Vd);
let regs : integer = if Q == ‘0’ then 1 else 2;
```

###### T4

![](images/6060-F6.1.134-VMOV-immediate-img09.svg)

###### Encoding for the 64-bit SIMD vector variant

Applies when `(Q == 0)`

```
VMOV{<c>}{<q>}.<dt>  <Dd>, #<imm>
```

###### Encoding for the 128-bit SIMD vector variant

Applies when `(Q == 1)`

```
VMOV{<c>}{<q>}.<dt>  <Qd>, #<imm>
```

###### Decode for all variants of this encoding

```
if op == ‘0’ && cmode[0] == ‘1’ && cmode[3:2] != ‘11’ then See(“VORR (immediate)”); end;
if op == ‘1’ && cmode != ‘1110’ then See(“Related encodings”); end;
if Q == ‘1’ && Vd[0] == ‘1’ then Undefined(); end;
let advsimd : boolean = TRUE;
let esize : integer{} = 64;
let imm : bits(esize) = AdvSIMDExpandImm(op, cmode, i::imm3::imm4);
let d : integer = UInt(D::Vd);
let regs : integer = if Q == ‘0’ then 1 else 2;
```

###### T5

![](images/6060-F6.1.134-VMOV-immediate-img10.svg)

###### Encoding for the 64-bit SIMD vector variant

Applies when `(Q == 0)`

```
VMOV{<c>}{<q>}.I64  <Dd>, #<imm>
```

###### Encoding for the 128-bit SIMD vector variant

Applies when `(Q == 1)`

```
VMOV{<c>}{<q>}.I64  <Qd>, #<imm>
```

###### Decode for all variants of this encoding

```
if op == ‘0’ && cmode[0] == ‘1’ && cmode[3:2] != ‘11’ then See(“VORR (immediate)”); end;
if op == ‘1’ && cmode != ‘1110’ then See(“Related encodings”); end;
if Q == ‘1’ && Vd[0] == ‘1’ then Undefined(); end;
let advsimd : boolean = TRUE;
let esize : integer{} = 64;
let imm : bits(esize) = AdvSIMDExpandImm(op, cmode, i::imm3::imm4);
let d : integer = UInt(D::Vd);
let regs : integer = if Q == ‘0’ then 1 else 2;
```

Related encodings: See [Advanced SIMD one register and modified immediate](/documentation/ddi0487/mc/-Part-F-The-AArch32-Instruction-Sets/-Chapter-F3-T32-Instruction-Set-Encoding/-F3-1-T32-instruction-set-encoding/-F3-1-10-Advanced-SIMD-shifts-and-immediate-generation?lang=en#t32_encoding_index_simd_1r_imm) for the T32 instruction set, or [Advanced SIMD one register and modified immediate](/documentation/ddi0487/mc/-Part-F-The-AArch32-Instruction-Sets/-Chapter-F4-A32-Instruction-Set-Encoding/-F4-1-A32-instruction-set-encoding/-F4-1-22-Advanced-SIMD-shifts-and-immediate-generation?lang=en#a32_encoding_index_simd1reg_imm) for the A32 instruction set.

###### Assembler Symbols

<c>
:   ​

    For the “A1 128-bit SIMD vector”, “A1 64-bit SIMD vector”, “A3 128-bit SIMD vector”, “A3 64-bit SIMD vector”, “A4 128-bit SIMD vector”, “A4 64-bit SIMD vector”, “A5 128-bit SIMD vector”, and “A5 64-bit SIMD vector” variants: see [Standard assembler syntax fields](/documentation/ddi0487/mc/-Part-F-The-AArch32-Instruction-Sets/-Chapter-F1-About-the-T32-and-A32-Instruction-Descriptions/-F1-2-Standard-assembler-syntax-fields?lang=en#babbefhf). This encoding must be unconditional.

    ​
    ​

    For the “A2 Double-precision scalar”, “A2 Half-precision scalar”, “A2 Single-precision scalar”, “T1 128-bit SIMD vector”, “T1 64-bit SIMD vector”, “T2 Double-precision scalar”, “T2 Half-precision scalar”, “T2 Single-precision scalar”, “T3 128-bit SIMD vector”, “T3 64-bit SIMD vector”, “T4 128-bit SIMD vector”, “T4 64-bit SIMD vector”, “T5 128-bit SIMD vector”, and “T5 64-bit SIMD vector” variants: see [Standard assembler syntax fields](/documentation/ddi0487/mc/-Part-F-The-AArch32-Instruction-Sets/-Chapter-F1-About-the-T32-and-A32-Instruction-Descriptions/-F1-2-Standard-assembler-syntax-fields?lang=en#babbefhf).

<q>
:   ​
    ​

    See [Standard assembler syntax fields](/documentation/ddi0487/mc/-Part-F-The-AArch32-Instruction-Sets/-Chapter-F1-About-the-T32-and-A32-Instruction-Descriptions/-F1-2-Standard-assembler-syntax-fields?lang=en#babbefhf).

<Dd>
:   ​

    Is the 64-bit name of the SIMD&FP destination register, encoded in the “D:Vd” field.

<imm>
:   ​

    For the “A1 128-bit SIMD vector”, “A1 64-bit SIMD vector”, “A3 128-bit SIMD vector”, “A3 64-bit SIMD vector”, “A4 128-bit SIMD vector”, “A4 64-bit SIMD vector”, “A5 128-bit SIMD vector”, “A5 64-bit SIMD vector”, “T1 128-bit SIMD vector”, “T1 64-bit SIMD vector”, “T3 128-bit SIMD vector”, “T3 64-bit SIMD vector”, “T4 128-bit SIMD vector”, “T4 64-bit SIMD vector”, “T5 128-bit SIMD vector”, and “T5 64-bit SIMD vector” variants: is a constant of the specified type that is replicated to fill the destination register. For details of the range of constants available and the encoding of `<imm>`, see [Modified immediate constants in T32 and A32 Advanced SIMD instructions](/documentation/ddi0487/mc/-Part-F-The-AArch32-Instruction-Sets/-Chapter-F1-About-the-T32-and-A32-Instruction-Descriptions/-F1-7-General-information-about-the-T32-and-A32-instruction-descriptions/-F1-7-7-Modified-immediate-constants-in-T32-and-A32-instructions?lang=en#cjaidjdj).

    ​

    For the “A2 Double-precision scalar”, “A2 Half-precision scalar”, “A2 Single-precision scalar”, “T2 Double-precision scalar”, “T2 Half-precision scalar”, and “T2 Single-precision scalar” variants: is a signed floating-point constant with 3-bit exponent and normalized 4 bits of precision, encoded in “imm4H:imm4L”. For details of the range of constants available and the encoding of `<imm>`, see [Modified immediate constants in T32 and A32 floating-point instructions](/documentation/ddi0487/mc/-Part-F-The-AArch32-Instruction-Sets/-Chapter-F1-About-the-T32-and-A32-Instruction-Descriptions/-F1-7-General-information-about-the-T32-and-A32-instruction-descriptions/-F1-7-7-Modified-immediate-constants-in-T32-and-A32-instructions?lang=en#cjajbhcg).

<Qd>
:   ​

    Is the 128-bit name of the SIMD&FP destination register, encoded in the “D:Vd” field as <Qd>\*2.

<Sd>
:   ​

    Is the 32-bit name of the SIMD&FP destination register, encoded in the “Vd:D” field.

<dt>
:   ​

    The data type, encoded in “cmode”:

<table>
 <colgroup>
  <col/>
  <col/>
 </colgroup>
 <thead>
  <tr>
   <th>
    <strong>
     cmode
    </strong>
   </th>
   <th>
    <strong>
     &lt;dt&gt;
    </strong>
   </th>
  </tr>
 </thead>
 <tbody>
  <tr>
   <td>
    <p>
     <code>
      110x
     </code>
    </p>
   </td>
   <td>
    <p>
     <code>
      I32
     </code>
    </p>
   </td>
  </tr>
  <tr>
   <td>
    <p>
     <code>
      1110
     </code>
    </p>
   </td>
   <td>
    <p>
     <code>
      I8
     </code>
    </p>
   </td>
  </tr>
  <tr>
   <td>
    <p>
     <code>
      1111
     </code>
    </p>
   </td>
   <td>
    <p>
     <code>
      F32
     </code>
    </p>
   </td>
  </tr>
 </tbody>
</table>

###### Operation

```
if ConditionPassed() then
    EncodingSpecificOperations();
    CheckAdvSIMDOrVFPEnabled(TRUE, advsimd);
    if esize <= 32 then
        S(d) = ZeroExtend{32}(imm);
    else
        for r = 0 to regs-1 do
            D(d+r) = ZeroExtend{64}(imm);
        end;
    end;
end;
```

###### Operational Information

This instruction is a data-independent-time instruction as described in [About the DIT bit](/documentation/ddi0487/mc/-Part-E-The-AArch32-Application-Level-Architecture/-Chapter-E1-The-AArch32-Application-Level-Programmers--Model/-E1-2-The-Application-level-programmers--model-in-AArch32-state/-E1-2-5-About-the-DIT-bit?lang=en#beiidceg).
