# ​J1.2.566 AArch64_ChooseNonExcludedTag

Source: <https://developer.arm.com/documentation/ddi0487/mc/-Part-J-Architectural-Pseudocode/-Chapter-J1-A-profile-Architecture-Pseudocode/-J1-2-Pseudocode-for-AArch64-operation/-J1-2-566-AArch64-ChooseNonExcludedTag>

##### J1.2.566 AArch64\_ChooseNonExcludedTag

```
// AArch64_ChooseNonExcludedTag()
// ==============================
// Return a tag derived from the start and the offset values, excluding
// any tags in the given mask.
func AArch64_ChooseNonExcludedTag(tag_in : bits(4), offset_in : bits(4),
                                  exclude : bits(16)) => bits(4)
begin
    var tag : bits(4) = tag_in;
    var offset : bits(4) = offset_in;
    if IsOnes(exclude) then
        return ‘0000’;
    end;
    if offset == ‘0000’ then
        while exclude[UInt(tag)] == ‘1’ looplimit 16 do
            tag = tag + ‘0001’;
        end;
    end;
    while offset != ‘0000’ looplimit 16 do
        offset = offset - ‘0001’;
        tag = tag + ‘0001’;
        while exclude[UInt(tag)] == ‘1’ looplimit 16 do
            tag = tag + ‘0001’;
        end;
    end;
    return tag;
end;
```
