# ​J1.4.520 ELIsInHost

Source: <https://developer.arm.com/documentation/ddi0487/mc/-Part-J-Architectural-Pseudocode/-Chapter-J1-A-profile-Architecture-Pseudocode/-J1-4-Shared-pseudocode/-J1-4-520-ELIsInHost>

##### J1.4.520 ELIsInHost

```
// ELIsInHost()
// ============
readonly func ELIsInHost(el : bits(2)) => boolean
begin
    if !IsFeatureImplemented(FEAT_VHE) || ELUsingAArch32(EL2) then
        return FALSE;
    end;
    case el of
        when EL3 =>
            return FALSE;
        when EL2 =>
            return EL2Enabled() && EffectiveHCR_EL2_E2H() == ‘1’;
        when EL1 =>
            return FALSE;
        when EL0 =>
            return EL2Enabled() && EffectiveHCR_EL2_E2H()::HCR_EL2().TGE == ‘11’;
        otherwise =>
            unreachable;
    end;
end;
```
