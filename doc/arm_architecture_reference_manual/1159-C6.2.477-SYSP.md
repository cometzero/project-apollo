# ​C6.2.477 SYSP

Source: <https://developer.arm.com/documentation/ddi0487/mc/-Part-C-The-AArch64-Instruction-Set/-Chapter-C6-A64-Base-Instruction-Descriptions/-C6-2-Alphabetical-list-of-A64-base-instructions/-C6-2-477-SYSP>

##### C6.2.477 SYSP

128-bit system instruction.

This instruction is used by the alias [TLBIP](/documentation/ddi0487/mc/-Part-C-The-AArch64-Instruction-Set/-Chapter-C6-A64-Base-Instruction-Descriptions/-C6-2-Alphabetical-list-of-A64-base-instructions/-C6-2-481-TLBIP?lang=en#isa_tlbip_sysp).

###### System

###### (FEAT\_SYSINSTR128)

![](images/1159-C6.2.477-SYSP-img01.svg)

###### Encoding

```
SYSP  #<op1>, <Cn>, <Cm>, #<op2>{, <Xt1>, <Xt2>}
```

###### Decode for this encoding

```
if !IsFeatureImplemented(FEAT_SYSINSTR128) then EndOfDecode(Decode_UNDEF); end;
if Rt[0] == ‘1’ && Rt != ‘11111’ then EndOfDecode(Decode_UNDEF); end;
let t : integer       = UInt(Rt);
let t2 : integer      = if t == 31 then 31 else t + 1;
let sys_L : bits(1)   = L;
let sys_op0 : bits(2) = ‘01’;
let sys_op1 : bits(3) = op1;
let sys_op2 : bits(3) = op2;
let sys_crn : bits(4) = CRn;
let sys_crm : bits(4) = CRm;
```

###### Assembler Symbols

<op1>
:   ​

    Is a 3-bit unsigned immediate, in the range 0 to 7, encoded in the “op1” field.

<Cn>
:   ​

    Is a name ‘Cn’, with ‘n’ in the range 0 to 15, encoded in the “CRn” field.

<Cm>
:   ​

    Is a name ‘Cm’, with ‘m’ in the range 0 to 15, encoded in the “CRm” field.

<op2>
:   ​

    Is a 3-bit unsigned immediate, in the range 0 to 7, encoded in the “op2” field.

<Xt1>
:   ​

    Is the 64-bit name of the first optional general-purpose source register, defaulting to ‘11111’, encoded in the “Rt” field.

<Xt2>
:   ​

    Is the 64-bit name of the second optional general-purpose source register, defaulting to ‘11111’, encoded as “Rt” +1. Defaults to ‘11111’ if “Rt” = ‘11111’.

###### Alias Conditions

<table>
 <colgroup>
  <col/>
  <col/>
 </colgroup>
 <thead>
  <tr>
   <th>
    <strong>
     Alias
    </strong>
   </th>
   <th>
    <strong>
     Is preferred when
    </strong>
   </th>
  </tr>
 </thead>
 <tbody>
  <tr>
   <td>
    <p>
     <code>
      TLBIP
     </code>
    </p>
   </td>
   <td>
    <p>
     <code>
      CRn IN {'100x'} &amp;&amp; SysOp128(op1, CRn, CRm, op2) == Sys_TLBIP
     </code>
    </p>
   </td>
  </tr>
 </tbody>
</table>

###### Operation

```
AArch64_SysInstr128(sys_op0, sys_op1, sys_crn, sys_crm, sys_op2, t, t2);
```
