# ​J1.2.495 FPExpA

Source: <https://developer.arm.com/documentation/ddi0487/mc/-Part-J-Architectural-Pseudocode/-Chapter-J1-A-profile-Architecture-Pseudocode/-J1-2-Pseudocode-for-AArch64-operation/-J1-2-495-FPExpA>

##### J1.2.495 FPExpA

```
// FPExpA()
// ========
func FPExpA{N}(op : bits(N)) => bits(N)
begin
    assert N IN {16,32,64};
    var result : bits(N);
    var coeff : bits(N);
    let idx : integer = if N == 16 then UInt(op[4:0]) else UInt(op[5:0]);
    coeff = FPExpCoefficient{N}(idx);
    if N == 16 then
        result[15:0] = ‘0’::op[9:5]::coeff[9:0];
    elsif N == 32 then
        result[31:0] = ‘0’::op[13:6]::coeff[22:0];
    else // N == 64
        result[63:0] = ‘0’::op[16:6]::coeff[51:0];
    end;
    return result;
end;
```
