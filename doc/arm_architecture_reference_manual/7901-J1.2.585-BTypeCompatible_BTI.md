# ​J1.2.585 BTypeCompatible_BTI

Source: <https://developer.arm.com/documentation/ddi0487/mc/-Part-J-Architectural-Pseudocode/-Chapter-J1-A-profile-Architecture-Pseudocode/-J1-2-Pseudocode-for-AArch64-operation/-J1-2-585-BTypeCompatible-BTI>

##### J1.2.585 BTypeCompatible\_BTI

```
// BTypeCompatible_BTI
// ===================
// This function determines whether a given hint encoding is compatible with the current value of
// PSTATE.BTYPE. A value of TRUE here indicates a valid Branch Target Identification instruction.
func BTypeCompatible_BTI(hintcode : bits(2)) => boolean
begin
    case hintcode of
        when ‘00’ =>
            return PSTATE.BTYPE == ‘00’;
        when ‘01’ =>
            return PSTATE.BTYPE != ‘11’;
        when ‘10’ =>
            return PSTATE.BTYPE != ‘10’;
        when ‘11’ =>
            return TRUE;
    end;
end;
```
