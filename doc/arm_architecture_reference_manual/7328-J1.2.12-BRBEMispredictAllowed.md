# ​J1.2.12 BRBEMispredictAllowed

Source: <https://developer.arm.com/documentation/ddi0487/mc/-Part-J-Architectural-Pseudocode/-Chapter-J1-A-profile-Architecture-Pseudocode/-J1-2-Pseudocode-for-AArch64-operation/-J1-2-12-BRBEMispredictAllowed>

##### J1.2.12 BRBEMispredictAllowed

```
// BRBEMispredictAllowed()
// =======================
// Returns TRUE if the recording of branch misprediction is allowed,
// FALSE otherwise.
func BRBEMispredictAllowed() => boolean
begin
    if HaveEL(EL2) && BRBCR_EL2().MPRED == ‘0’ then return FALSE; end;
    if BRBCR_EL1().MPRED == ‘0’ then return FALSE; end;
    return TRUE;
end;
```
