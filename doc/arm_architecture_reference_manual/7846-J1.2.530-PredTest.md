# ​J1.2.530 PredTest

Source: <https://developer.arm.com/documentation/ddi0487/mc/-Part-J-Architectural-Pseudocode/-Chapter-J1-A-profile-Architecture-Pseudocode/-J1-2-Pseudocode-for-AArch64-operation/-J1-2-530-PredTest>

##### J1.2.530 PredTest

```
// PredTest()
// ==========
func PredTest{N}(mask : bits(N), result : bits(N), esize : integer) => bits(4)
begin
    let n : bit = FirstActive{N}(mask, result, esize);
    let z : bit = NoneActive{N}(mask, result, esize);
    let c : bit = NOT LastActive{N}(mask, result, esize);
    let v : bit = ‘0’;
    return n::z::c::v;
end;
```
