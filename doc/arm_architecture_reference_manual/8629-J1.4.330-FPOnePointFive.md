# ​J1.4.330 FPOnePointFive

Source: <https://developer.arm.com/documentation/ddi0487/mc/-Part-J-Architectural-Pseudocode/-Chapter-J1-A-profile-Architecture-Pseudocode/-J1-4-Shared-pseudocode/-J1-4-330-FPOnePointFive>

##### J1.4.330 FPOnePointFive

```
// FPOnePointFive()
// ================
func FPOnePointFive{N}(sign : bit) => bits(N)
begin
    assert N IN {16,32,64};
    let E : integer{}  = (if N == 16 then 5 else (if N == 32 then 8 else 11));
    let F : integer{}  = N - (E + 1);
    let exp : bits(E)  = ‘0’::Ones{E-1};
    let frac : bits(F) = ‘1’::Zeros{F-1};
    let result : bits((F + E) + 1) = sign :: exp :: frac;
    return result;
end;
```
