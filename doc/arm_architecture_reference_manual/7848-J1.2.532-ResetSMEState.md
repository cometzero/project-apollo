# ​J1.2.532 ResetSMEState

Source: <https://developer.arm.com/documentation/ddi0487/mc/-Part-J-Architectural-Pseudocode/-Chapter-J1-A-profile-Architecture-Pseudocode/-J1-2-Pseudocode-for-AArch64-operation/-J1-2-532-ResetSMEState>

##### J1.2.532 ResetSMEState

```
// ResetSMEState()
// ===============
func ResetSMEState(newenable : bit)
begin
    let vectors : integer = MAX_VL DIV 8;
    if newenable == ‘1’ then
        for n = 0 to vectors - 1 do
            _ZA[[n]] = Zeros{MAX_VL};
        end;
        if IsFeatureImplemented(FEAT_SME2) then
            _ZT0 = Zeros{ZT0_LEN};
        end;
    else
        for n = 0 to vectors - 1 do
            _ZA[[n]] = ARBITRARY : bits(MAX_VL);
        end;
        if IsFeatureImplemented(FEAT_SME2) then
            _ZT0 = ARBITRARY : bits(ZT0_LEN);
        end;
    end;
end;
```
