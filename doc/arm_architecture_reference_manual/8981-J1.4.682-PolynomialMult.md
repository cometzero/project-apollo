# ​J1.4.682 PolynomialMult

Source: <https://developer.arm.com/documentation/ddi0487/mc/-Part-J-Architectural-Pseudocode/-Chapter-J1-A-profile-Architecture-Pseudocode/-J1-4-Shared-pseudocode/-J1-4-682-PolynomialMult>

##### J1.4.682 PolynomialMult

```
// PolynomialMult()
// ================
func PolynomialMult{M, N}(op1 : bits(M), op2 : bits(N)) => bits(M+N)
begin
    var result : bits(N + M) = Zeros{M+N};
    let extended_op2 : bits(N + M) = ZeroExtend{M+N}(op2);
    for i=0 to M-1 do
        if op1[i] == ‘1’ then
            result = result XOR LSL(extended_op2, i);
        end;
    end;
    return result;
end;
```
