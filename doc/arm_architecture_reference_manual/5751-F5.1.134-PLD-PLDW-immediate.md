# ​F5.1.134 PLD, PLDW (immediate)

Source: <https://developer.arm.com/documentation/ddi0487/mc/-Part-F-The-AArch32-Instruction-Sets/-Chapter-F5-T32-and-A32-Base-Instruction-Set-Instruction-Descriptions/-F5-1-Alphabetical-list-of-T32-and-A32-base-instruction-set-instructions/-F5-1-134-PLD--PLDW--immediate->

##### F5.1.134 PLD, PLDW (immediate)

Preload Data (immediate) signals the memory system that data memory accesses from a specified address are likely in the near future. The memory system can respond by taking actions that are expected to speed up the memory accesses when they do occur, such as preloading the cache line containing the specified address into the data cache.

The `PLD` instruction signals that the likely memory access is a read, and the `PLDW` instruction signals that it is a write.

The effect of a `PLD` or `PLDW` instruction is IMPLEMENTATION DEFINED. For more information, see [Preloading caches](/documentation/ddi0487/mc/-Part-E-The-AArch32-Application-Level-Architecture/-Chapter-E2-The-AArch32-Application-Level-Memory-Model/-E2-5-Caches-and-memory-hierarchy/-E2-5-4-Prefetching-into-cache?lang=en#cegjjfca).

It has encodings from the following instruction sets: A32 ([A1](/documentation/ddi0487/mc/-Part-F-The-AArch32-Instruction-Sets/-Chapter-F5-T32-and-A32-Base-Instruction-Set-Instruction-Descriptions/-F5-1-Alphabetical-list-of-T32-and-A32-base-instruction-set-instructions/-F5-1-134-PLD--PLDW--immediate-?lang=en#isa_pld_i_iclass_a1)) and T32 ([T1](/documentation/ddi0487/mc/-Part-F-The-AArch32-Instruction-Sets/-Chapter-F5-T32-and-A32-Base-Instruction-Set-Instruction-Descriptions/-F5-1-Alphabetical-list-of-T32-and-A32-base-instruction-set-instructions/-F5-1-134-PLD--PLDW--immediate-?lang=en#isa_pld_i_iclass_t1) and [T2](/documentation/ddi0487/mc/-Part-F-The-AArch32-Instruction-Sets/-Chapter-F5-T32-and-A32-Base-Instruction-Set-Instruction-Descriptions/-F5-1-Alphabetical-list-of-T32-and-A32-base-instruction-set-instructions/-F5-1-134-PLD--PLDW--immediate-?lang=en#isa_pld_i_iclass_t2)).

###### A1

![](images/5751-F5.1.134-PLD-PLDW-immediate-img01.svg)

###### Encoding for the Preload read variant

Applies when `(R == 1)`

```
PLD{<c>}{<q>}  [<Rn> {, #{+/-}<imm>}]
```

###### Encoding for the Preload write variant

Applies when `(R == 0)`

```
PLDW{<c>}{<q>}  [<Rn> {, #{+/-}<imm>}]
```

###### Decode for all variants of this encoding

```
if Rn == ‘1111’ then See(“PLD (literal)”); end;
let n : integer = UInt(Rn);
let imm32 : bits(32) = ZeroExtend{}(imm12);
let add : boolean = (U == ‘1’);
let is_pldw : boolean = (R == ‘0’);
```

###### T1

![](images/5751-F5.1.134-PLD-PLDW-immediate-img02.svg)

###### Encoding for the Preload read variant

Applies when `(W == 0)`

```
PLD{<c>}{<q>}  [<Rn> {, #{+}<imm>}]
```

###### Encoding for the Preload write variant

Applies when `(W == 1)`

```
PLDW{<c>}{<q>}  [<Rn> {, #{+}<imm>}]
```

###### Decode for all variants of this encoding

```
if Rn == ‘1111’ then See(“PLD (literal)”); end;
let n : integer = UInt(Rn);
let imm32 : bits(32) = ZeroExtend{}(imm12);
let add : boolean = TRUE;
let is_pldw : boolean = (W == ‘1’);
```

###### T2

![](images/5751-F5.1.134-PLD-PLDW-immediate-img03.svg)

###### Encoding for the Preload read variant

Applies when `(W == 0)`

```
PLD{<c>}{<q>}  [<Rn> {, #-<imm>}]
```

###### Encoding for the Preload write variant

Applies when `(W == 1)`

```
PLDW{<c>}{<q>}  [<Rn> {, #-<imm>}]
```

###### Decode for all variants of this encoding

```
if Rn == ‘1111’ then See(“PLD (literal)”); end;
let n : integer = UInt(Rn);
let imm32 : bits(32) = ZeroExtend{}(imm8);
let add : boolean = FALSE;
let is_pldw : boolean = (W == ‘1’);
```

For more information about the CONSTRAINED UNPREDICTABLE behavior of this instruction, see [Architectural Constraints on UNPREDICTABLE behaviors](/documentation/ddi0487/mc/-Part-K-Appendixes/-Appendix-K1-Architectural-Constraints-on-UNPREDICTABLE-Behaviors?lang=en#cjaegdjc).

###### Assembler Symbols

<c>
:   ​

    For the “A1 Preload read” and “A1 Preload write” variants: see [Standard assembler syntax fields](/documentation/ddi0487/mc/-Part-F-The-AArch32-Instruction-Sets/-Chapter-F1-About-the-T32-and-A32-Instruction-Descriptions/-F1-2-Standard-assembler-syntax-fields?lang=en#babbefhf). Must be `AL` or omitted.

    ​

    For the “T1 Preload read”, “T1 Preload write”, “T2 Preload read”, and “T2 Preload write” variants: see [Standard assembler syntax fields](/documentation/ddi0487/mc/-Part-F-The-AArch32-Instruction-Sets/-Chapter-F1-About-the-T32-and-A32-Instruction-Descriptions/-F1-2-Standard-assembler-syntax-fields?lang=en#babbefhf).

<q>
:   ​
    ​

    See [Standard assembler syntax fields](/documentation/ddi0487/mc/-Part-F-The-AArch32-Instruction-Sets/-Chapter-F1-About-the-T32-and-A32-Instruction-Descriptions/-F1-2-Standard-assembler-syntax-fields?lang=en#babbefhf).

<Rn>
:   ​

    Is the general-purpose base register, encoded in the “Rn” field. If the PC is used, see [PLD (literal)](/documentation/ddi0487/mc/-Part-F-The-AArch32-Instruction-Sets/-Chapter-F5-T32-and-A32-Base-Instruction-Set-Instruction-Descriptions/-F5-1-Alphabetical-list-of-T32-and-A32-base-instruction-set-instructions/-F5-1-135-PLD--literal-?lang=en#isa_pld_l).

+/-
:   ​

    Specifies the offset is added to or subtracted from the base register, defaulting to + if omitted and encoded in “U”:

<table>
 <colgroup>
  <col/>
  <col/>
 </colgroup>
 <thead>
  <tr>
   <th>
    <strong>
     U
    </strong>
   </th>
   <th>
    <strong>
     +/-
    </strong>
   </th>
  </tr>
 </thead>
 <tbody>
  <tr>
   <td>
    <p>
     <code>
      0
     </code>
    </p>
   </td>
   <td>
    <p>
     <code>
      -
     </code>
    </p>
   </td>
  </tr>
  <tr>
   <td>
    <p>
     <code>
      1
     </code>
    </p>
   </td>
   <td>
    <p>
     <code>
      +
     </code>
    </p>
   </td>
  </tr>
 </tbody>
</table>

<imm>
:   ​

    For the “A1 Preload read” and “A1 Preload write” variants: is the optional 12-bit unsigned immediate byte offset, in the range 0 to 4095, defaulting to 0 and encoded in the “imm12” field.

    ​

    For the “T1 Preload read” and “T1 Preload write” variants: is an optional 12-bit unsigned immediate byte offset, in the range 0 to 4095, defaulting to 0 and encoded in the “imm12” field.

    ​

    For the “T2 Preload read” and “T2 Preload write” variants: is an 8-bit unsigned immediate byte offset, in the range 0 to 255, defaulting to 0 if omitted, and encoded in the “imm8” field.

+
:   ​

    Specifies the offset is added to the base register.

###### Operation

```
if ConditionPassed() then
    EncodingSpecificOperations();
    let address : bits(32) = if add then (R(n) + imm32) else (R(n) - imm32);
    if is_pldw then
        Hint_PreloadDataForWrite(address);
    else
        Hint_PreloadData(address);
    end;
end;
```
