# ​J1.2.504 FPTrigSMul

Source: <https://developer.arm.com/documentation/ddi0487/mc/-Part-J-Architectural-Pseudocode/-Chapter-J1-A-profile-Architecture-Pseudocode/-J1-2-Pseudocode-for-AArch64-operation/-J1-2-504-FPTrigSMul>

##### J1.2.504 FPTrigSMul

```
// FPTrigSMul()
// ============
func FPTrigSMul{N}(op1 : bits(N), op2 : bits(N), fpcr : FPCR_Type) => bits(N)
begin
    assert N IN {16,32,64};
    var result : bits(N) = FPMul{}(op1, op1, fpcr);
    let fpexc = FALSE;
    let (fptype, sign, value) : (FPType, bit, real) = FPUnpack{N}(result, fpcr, fpexc);
    if ! fptype IN {FPType_QNaN, FPType_SNaN} then
        result[N-1] = op2[0];
    end;
    return result;
end;
```
