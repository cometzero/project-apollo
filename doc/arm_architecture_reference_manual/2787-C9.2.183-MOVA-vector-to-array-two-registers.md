# ​C9.2.183 MOVA (vector to array, two registers)

Source: <https://developer.arm.com/documentation/ddi0487/mc/-Part-C-The-AArch64-Instruction-Set/-Chapter-C9-SME-Instruction-Descriptions/-C9-2-Alphabetical-list-of-SME-instructions/-C9-2-183-MOVA--vector-to-array--two-registers->

##### C9.2.183 MOVA (vector to array, two registers)

Move Z two-vector operand to two ZA single-vector groups

This instruction operates on two ZA single-vector groups.

The single-vector group within each half of the ZA array is selected by the sum of the vector select register and offset, modulo half the number of ZA array vectors.

The `vector group` symbol `VGx2` indicates that the instruction operates on two ZA single-vector groups.

The preferred disassembly syntax uses a 64-bit element size, but an assembler should accept any element size if it is used consistently for all operands. The `vector group` symbol is preferred for disassembly, but optional in assembler source code.

This instruction is unpredicated.

This instruction is used by the alias [MOV (vector to array, two registers)](/documentation/ddi0487/mc/-Part-C-The-AArch64-Instruction-Set/-Chapter-C9-SME-Instruction-Descriptions/-C9-2-Alphabetical-list-of-SME-instructions/-C9-2-173-MOV--vector-to-array--two-registers-?lang=en#isa_mov_mova_za_mz2).

###### SME2

###### (FEAT\_SME2)

![](images/2787-C9.2.183-MOVA-vector-to-array-two-registers-img01.svg)

###### Encoding

```
MOVA  ZA.D[<Wv>, <offs>{, VGx2}], { <Zn1>.D-<Zn2>.D }
```

###### Decode for this encoding

```
if !IsFeatureImplemented(FEAT_SME2) then EndOfDecode(Decode_UNDEF); end;
let v : integer = UInt(‘010’::Rv);
let offset : integer = UInt(off3);
let n : integer = UInt(Zn::‘0’);
let nreg : integer{} = 2;
```

###### Assembler Symbols

<Wv>
:   ​

    Is the 32-bit name of the vector select register W8-W11, encoded as “Rv” plus 8.

<offs>
:   ​

    Is the vector select offset, in the range 0 to 7, encoded in the “off3” field.

<Zn1>
:   ​

    Is the name of the first scalable vector register of the source multi-vector group, encoded as “Zn” times 2.

<Zn2>
:   ​

    Is the name of the second scalable vector register of the source multi-vector group, encoded as “Zn” times 2 plus 1.

###### Alias Conditions

<table>
 <colgroup>
  <col/>
  <col/>
 </colgroup>
 <thead>
  <tr>
   <th>
    <strong>
     Alias
    </strong>
   </th>
   <th>
    <strong>
     Is preferred when
    </strong>
   </th>
  </tr>
 </thead>
 <tbody>
  <tr>
   <td>
    <p>
     <code>
      MOV (vector to array, two registers)
     </code>
    </p>
   </td>
   <td>
    <p>
     <code>
      Unconditionally
     </code>
    </p>
   </td>
  </tr>
 </tbody>
</table>

###### Operation

```
CheckStreamingSVEAndZAEnabled();
let VL : integer{} = CurrentVL();
let vectors : integer = VL DIV 8;
let vstride : integer = vectors DIV nreg;
let vbase : bits(32) = X{}(v);
var vec : integer = (UInt(vbase) + offset) MOD vstride;
for r = 0 to nreg-1 do
    let result : bits(VL) = Z{}(n + r);
    ZAvector{VL}(vec) = result;
    vec = vec + vstride;
end;
```

###### Operational Information

This instruction is a data-independent-time instruction as described in [About PSTATE.DIT](/documentation/ddi0487/mc/-Part-B-The-AArch64-Application-Level-Architecture/-Chapter-B1-The-AArch64-Application-Level-Programmers--Model/-B1-5-Software-control-features-and-EL0/-B1-5-6-About-PSTATE-DIT?lang=en#beiccddab3).
