# ​J1.2.293 AArch64_IsUnprivAccessPriv

Source: <https://developer.arm.com/documentation/ddi0487/mc/-Part-J-Architectural-Pseudocode/-Chapter-J1-A-profile-Architecture-Pseudocode/-J1-2-Pseudocode-for-AArch64-operation/-J1-2-293-AArch64-IsUnprivAccessPriv>

##### J1.2.293 AArch64\_IsUnprivAccessPriv

```
// AArch64_IsUnprivAccessPriv()
// ============================
// Returns TRUE if an unprivileged access is privileged, and FALSE otherwise.
func AArch64_IsUnprivAccessPriv() => boolean
begin
    var privileged : boolean;
    case PSTATE.EL of
        when EL0 =>
            privileged = FALSE;
        when EL1 => privileged = EffectiveHCR_EL2_NVx()[1:0] == ‘11’;
        when EL2 => privileged = !ELIsInHost(EL0);
        when EL3 =>
            privileged = TRUE;
    end;
    if IsFeatureImplemented(FEAT_UAO) && PSTATE.UAO == ‘1’ then
        privileged = PSTATE.EL != EL0;
    end;
    return privileged;
end;
```
