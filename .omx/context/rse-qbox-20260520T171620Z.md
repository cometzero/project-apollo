# RSE-QBox Team Context Snapshot

Created: 2026-05-20T17:16:20Z

## Task Statement

Create an OMX `$team` and continue implementing `doc/spec/rse-qbox` according
to the spec/task list until the RSE-oriented QBox path is implemented and
verified.

## Desired Outcome

- QBox RSE-oriented boot progresses according to `doc/spec/rse-qbox/`.
- Each completed task has file-backed evidence in `build/qbox-fvp-rd-aspen/`.
- Verification uses logs and JSON reports, not tmux screen output.
- Final state distinguishes MVP completion from skeleton/blocker evidence.

## Current Baseline

- Workspace: `/build/arm/arm-auto-solutions`
- Root is not a single Git repository; `tools/qbox` is the active nested repo.
- `.config.yaml` records `MACHINE = "fvp-rd-aspen"`,
  `RD_ASPEN_VARIANT = "cfg2"`, `PC_CPUS_COUNT_DEFAULT = "4"`, and
  `ARCHITECTURE_BAREMETAL = true`.
- Active QBox platform: `tools/qbox/platforms/fvp-rd-aspen-rse/conf.lua`.
- Current open task from `doc/spec/rse-qbox/task.md`: T019F, identify the
  post-system-control no-console blocker after BL1_1 writes
  `reset_mask = 0x100`.

## Known Evidence

- T019E added `tools/qbox/systemc-components/rse_sysctrl/` and
  `tools/qbox/tests/components/rse_sysctrl/`.
- The previous `0x58021100` reset-syndrome Data Abort is removed.
- Runtime evidence roots:
  - `build/qbox-fvp-rd-aspen/rse-sysctrl-20260521-v1/`
  - `build/qbox-fvp-rd-aspen/rse-sysctrl-20260521-v2/`
- `rse-sysctrl-20260521-v2/result.json` reports:
  - `passed: false`
  - `blocker: qbox_platform_timeout`
  - `first_failing_register_access: null`
  - `fidelity_labels.rse_sysctrl = "touched-register-model"`
- `qbox-platform.log` shows:
  - `platform.rse_sysctrl read offset=0x100 len=0x4 value=0x80000000`
  - `platform.rse_sysctrl read offset=0x104 len=0x4 value=0x0`
  - `platform.rse_sysctrl write offset=0x104 len=0x4 value=0x100`

## Constraints

- Preserve user changes; do not reset nested repos.
- Keep edits scoped to owning repo/project-local docs.
- Prefer SystemC/TLM/libqemu behavior over stubs.
- Avoid broad recursive scans of `build/`; inspect targeted TF-M sources and
  runtime logs only.
- Do not claim RSE boot success until required TF-M/RSE, SCMI, measured boot,
  AP release, and Linux markers are present.

## Likely Touchpoints

- `doc/spec/rse-qbox/task.md`
- `doc/spec/rse-qbox/evidence.md`
- `doc/spec/rse-qbox/design.md`
- `doc/spec/rse-qbox/plan.md`
- `tools/qbox/platforms/fvp-rd-aspen-rse/conf.lua`
- `tools/qbox/systemc-components/rse_sysctrl/`
- `tools/qbox/systemc-components/dma350/`
- `scripts/run_qbox_fvp_rd_aspen_rse.py`
- targeted TF-M sources under
  `build/tmp_baremetal/work/fvp_rd_aspen-poky-linux/trusted-firmware-m/`

## Open Questions For Team

- Which PC/source location repeats after `reset_mask = 0x100` when QEMU trace
  is enabled long enough?
- Is the next blocker boot media/provisioning, LCM/OTP, ATU/MPC, reset
  lifecycle side effects, or another touched register?
- What is the narrowest next SystemC/TLM model increment that moves the
  blocker without hiding fidelity gaps?
